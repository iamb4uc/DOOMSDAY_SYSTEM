name: check

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  void:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Void checks
        run: make void
